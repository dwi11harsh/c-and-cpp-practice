/* without assigning any value to a variable adding a random number to it
and then taking the output
coz before we saw that mac has a garbage vlaue of one */

#include<stdio.h>
int main()
{
    int i=15, sum;
    sum = sum + i;
    printf("\n%d\n", sum);

}

/* in the output we saw that mac has a non one garbage value 
for such programs */