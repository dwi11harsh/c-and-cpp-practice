/* SUBARRAY - continuous part of an array
 no of subarray of an array having n elements = n*(n+1)/2;


 SUBSEQUENCE - it is a sequence that can be derived from an array
 by selectiong zero or more elements without changing the order of the remaining elements;
 no of subsequence of an array with n elements = pow( 2 , n )
 
 every subarray is a subsequence, but not every subsequence is not a subarray
  */
